/* Nombre Fichero: include/potencia.h */

#include <stdlib.h>
#include <stdio.h>


// Declaracion de los prototipos de las funciones

/* funcion recursiva que calcula la potencia de un número elevado a n
 * [ IN] op, base de la potencia
 * [ IN] n, exponente
 * [RET] return, resultado de la operación op^n */
int potencia(int op, int n);